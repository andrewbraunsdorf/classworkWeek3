#include <stdio.h>
#include <stdlib.h>

\\ minichallenge create the NODE for a binary tree.

typedef struct NODE
{
    // identifer
    int value;
    struct NODE *left;
    struct NODE *right;
} NODE;
int main(void)
{
    printf("hello\n");
}